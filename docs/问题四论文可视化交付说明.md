# 问题四论文可视化交付说明

## 交付文件

- PCM 放热倍率与皮肤阈值时间反演曲线：
  - `paper/figures/problem4/problem4_lambda_t15_inverse.pdf`
  - `paper/figures/problem4/problem4_lambda_t15_inverse.png`
- PCM 最优放热倍率归一化敏感性：
  - `paper/figures/problem4/problem4_lambda_sensitivity.pdf`
  - `paper/figures/problem4/problem4_lambda_sensitivity.png`
- 可重复生成脚本：`src/可视化/plot_problem4_figures.py`

## 数据来源与补采样

- 图 1 读取 `problem4_search.csv` 和 `problem4_optimal_solution.csv`。原搜索文件只有 1–3 倍的 5 个粗点，因此脚本调用现有正式问题四 `solve_events` 接口，以正式 `Problem4Config.main_solver` 参数补充非均匀真实 ODE 采样；未复制或修改模型方程。
- 新增绘图接口 `results/outputs/problem4/problem4_plot_lambda_t15.csv`，共 47 行（46 个 `sample`、1 个 `optimal`），覆盖 `1 ≤ lambda_pcm ≤ 35`，并在正式最优根附近加密。
- 新 CSV 字段为 `lambda_pcm`、`improvement_pct`、`t15_min`、`gap_to_target_min`、`feasible`、`point_type`。
- 图 2 直接读取 `problem4_sensitivity_summary.csv`，未复制敏感度数据，也未加入离散情景参数。

## 图形与导出规格

- 两图画布均为 10.0 × 6.5 in；PNG 均为 300 DPI，即 3000 × 1950 px。
- PDF 页面均为 720 × 468 pt，保留矢量线条、文字和条形；检查未发现栅格图像对象。
- 两图遵循项目 `scientific-figure-making` skill 和已有绘图代码风格：华文中宋 18 pt、A/B 固定配色、弱化主网格、隐藏上/右边框、单面板无图内标题。PDF 已嵌入华文中宋字体。

## 一致性检查

- 正式最优倍率读取值为 `28.352528404007945`，与检查基准 `28.3525284040` 的绝对差为 `7.94e-12`。
- 正式目标时间读取值为 `734.8149675489651 min`；最优解达到 `734.8149675607893 min`，绝对差为 `1.18e-08 min`。水平线未使用未经重量惩罚的 `735.000828799 min`。
- 绘图采样的相邻最小增量为 `0.097015575 min`，未发现反向变化。
- 图 2 四项数据均存在且有限；按绝对值排序为 `h_in`、`M0`、`lambda_out`、`beta_DSC`，绘制值分别为 `+12.132620`、`-10.442551`、`+8.581804`、`+1.000000`，保留原符号。
- 自动化检查共 5 项，全部通过。
