\documentclass[withoutpreface,bwprint]{cumcmthesis}
\usepackage[framemethod=TikZ]{mdframed}
\usepackage{amstext} 
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{bm}
\usepackage{url}
\usepackage{subcaption}
\usepackage{mathtools} 
\usepackage[linesnumbered,ruled,lined]{algorithm2e}
\usepackage{makecell}
\usepackage{longtable}
\usepackage{metalogo}
\usepackage{setspace}

\usepackage{caption}
\usepackage{multirow}
\usepackage{float}
\usepackage{graphicx}
\usepackage[linesnumbered,ruled,lined]{algorithm2e}
\usepackage{threeparttable}
\usepackage[square,sort,comma,numbers]{natbib}
\usepackage{diagbox}
\usepackage{color}
\usepackage{transparent}
\usepackage{import}
\usepackage{hyperref}
\setlength{\cftsubsecnumwidth}{3.2em}
\setlength{\cftsubsubsecnumwidth}{3.8em}



%此部分用于预留常用代码
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%          伪代码
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\iffalse
\begin{algorithm}
		\caption{Function ImperialistCompetitveAlgorithm(x)}
		\LinesNumbered
		\textbf{Initialize}
		\tcc{初始化：设置多个初始国家解$X_n,n=1,...,n_max$。从中选取$N$个最好质量的解使其作为$N$个帝国的殖民国家，$X_1,X_2,...,X_N$, 为每个殖民国家分配殖民国家势力比例数量的殖民地得到$N$个帝国$E_1,E_2,...,E_N$}
		iteration = 1;
		\textbf{Repeat}
		\For{$i \in [1,N]$}{
			EmpireAssimilate($E_i$) \tcc{帝国同化}
			ColoniesRevolve($E_i$) \tcc{殖民地革命}
			EmpirePosses($E_i$) \tcc{殖民国家替换}
			CaculateNewCost($E_i$) \tcc{重新计算国家权力}
		}
		EmpireUnite({$E_1, E_2, \dots, E_N$}) \tcc{合并相似帝国}
		EmpireCompetition({$E_1,E_2,...,E_N$}) \tcc{帝国竞争，分配殖民地}
		iteration $\leftarrow$ iteration$+1$; {\mbox{进入下一次迭代}} \newline
		until \mbox{只有一个帝国剩下/达到迭代次数。}
	\end{algorithm}
\fi

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%          插图
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\iffalse
%         流程图
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/问题一流程图.pdf}
    \caption{问题一建模与求解流程图}
    \label{fig:question1-flowchart}
\end{figure}
%         背景图
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/.png}
    \caption{}
    \label{}
\end{figure}
\fi
\iffalse
%         两图并列
\begin{figure}[!h]
	\centering
	\begin{minipage}[c]{0.48\textwidth}
    	\centering
	    \includegraphics[width=1\textwidth]{}
	    \caption{}
	    \label{fig:}
    \end{minipage}
    \begin{minipage}[c]{0.48\textwidth}
		\centering
        \includegraphics[width=1\textwidth]{}
        \caption{}
        \label{fig:}
	\end{minipage}
\end{figure}
\fi
\iffalse
%       四幅子图
\begin{figure}[H]
	\centering
	\subfigure[]{\includegraphics[width=.43\textwidth]{}}
	\subfigure[]{\includegraphics[width=.43\textwidth]{}}\\
	\subfigure[]{\includegraphics[width=.43\textwidth]{}}
	\subfigure[]{\includegraphics[width=.43\textwidth]{}}
	\caption{}
	\label{fig:}
\end{figure}
\fi

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%           表格
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\iffalse
\begin{table}[!h]
	\caption{}
	\label{tab:}
	\centering
	\begin{tabular}{p{3cm}<{\centering}p{3cm}<{\centering}p{3cm}<{\centering}}
		\toprule[1.5pt]
		   &     &   \\ 
		\midrule[1pt]
		   &     &    \\
		\bottomrule[1.5pt]
	\end{tabular}
\end{table}
\fi
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%           公式
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\iffalse
\begin{equation}
    \left\{
         \begin{array}{lr}
       
       \frac{R}{\sin\alpha_1}=\frac{r}{\sin(\alpha_1+\varphi)}, \\
       \frac{R}{\sin\alpha_2}=\frac{r}{\sin(\alpha_2+\left|\theta -\varphi\right|)}.\\
 \end{array}
  \right.
 \end{equation}


\fi
\iffalse
\begin{aligned}
	\max & \sum_{j} X_{12 j} \times 1000+10000-\sum_{k}\left[\left(T_{k 1}+\sum_{j=2}^{27} 2 T_{k} j\right) a_{k}\right], \\
	\text { s.t. } & \left\{\begin{array}{l}
	\sum_{j=0}^{30} x_{27 j}=1, \\
	\sum_{j=0}^{30} x_{1 j} \geq 1, \\
	x_{i j} x_{i^{\prime} j+1} \leq d_{i i^{\prime}}, \\
	V_{j} \leq 2+M x_{12 j}, \\
	Q_{k j}+T_{k j}-S_{k j} \geq 0, \\
	Q_{k j+1}=Q_{k j}+T_{k j}-S_{k j}, \\
	\\
	3 Q_{1 j}+2 Q_{2 j} \leq 1200 .
	\end{array}\right.
	\end{aligned}
\fi




%\special{dvipdfmx:config z 0} %取消PDF压缩，加快速度，最终版本生成的时候最好把这句话注释掉
\title{基于动态热调节与多层传热的低温防护服性能预测及结构优化}
\begin{document}
\maketitle
\thispagestyle{empty}
\begin{abstract}\setlength{\parskip}{0.3\baselineskip}



\textbf{针对问题三，}以外层 $0.3\,\mathrm{mm}$ 涂层的总层数为整数决策变量，每层分别设置温度节点，
同时计入增厚带来的热阻、热容、质量和成本变化。
100 kg 作为不包含人体自重的总外部承重上限，
时间惩罚则仅作用于相对问题一的新增质量。
预算允许外层总层数 $N\in\{1,2,3,4\}$，
枚举得最优方案为在原外层上新增 3 层，
使总厚度达到 $1.2\,\mathrm{mm}$。此时防护服总质量为
 $1.500355\,\mathrm{kg}$，总成本为 $544.366950$ 元，
 重量修正后的主指标为 $734.815\,\mathrm{min}$，
 比问题一净增加 $56.194\,\mathrm{min}$。


    \keywords{\textbf{人体热调节} \quad \textbf{相变材料} \quad \textbf{动态皮肤血流} \quad \textbf{多层传热} \quad \textbf{整数优化} \quad \textbf{敏感性分析}}
\end{abstract}
\thispagestyle{empty}
\newpage
\setcounter{page}{1} 




\section{问题重述}
\subsection{问题背景}
低温防护服是保障寒冷环境下人体热安全的重要装备，其作用原理是利用多层材料阻滞
人体热量向外界传递，并通过相变材料在降温固化时释放潜热，从而延缓体表温度下降。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/nanjikekao.png}
    \caption{}
    \label{}
\end{figure}

在极地科考和寒冷地区野外作业中，防护效果受材料厚度与热物性、环境风速及人体活动
状态等因素影响，而增加厚度又受到负重能力和资金限制。因此，
需要预测不同工况下人体可安全进行户外活动的时间，
合理调整材料厚度和相变放热能力，以提高低温防护服的防护效果。
\subsection{问题提出}
针对低温防护服的传热与性能优化问题，具体研究如下：

\textbf{问题一：}实验者在南极洲长城站附近静止站立，环境温度为
$-40\,^{\circ}\mathrm C$，安静、晴天无风。分析人体、防护服与外界之间的传热过程，
建立热量传递模型，预测室外坚持时间。

\textbf{问题二：}当长城站外风速为 $3\,\mathrm{m/s}$ 且实验者进行轻微运动时，
在问题一模型中引入风速与人体活动的影响，计算实验者必须返回室内的大致时间。

\textbf{问题三：}沿用问题一的静止工况。已知实验者除自身体重外最多可承受
$100\,\mathrm{kg}$ 外部负重；以问题一最长坚持时间为基准，每多承受
$0.5\,\mathrm{kg}$ 使站立时间减少 $10\,\mathrm s$。新增材料费用不超过原支出的
$50\%$。在负重与资金约束下优化外层涂层厚度，使室外站立时间最长。

\textbf{问题四：}沿用问题三的厚度与资金条件，通过提高中间功能层的放热能力延长
户外活动时间。确定放热能力相对原材料至少提高的比例，使坚持时间不低于问题三结果。
\section{模型假设}
\begin{enumerate}
    \item 人体核心、皮肤及三层服装分别视为温度均匀的集中参数节点，热量只沿服装
    厚度方向传递。
    \item 实验者进入低温环境前已经穿着防护服，人体与三层服装达到近似热平衡。

    \item 针对 $-40\,^{\circ}\mathrm C$ 极寒低湿环境，
    假设环境水汽分压 $p_a=0\,\mathrm{Torr}$。
    \item 人体质量和等效比热保持不变；静止工况只计基础代谢产热与呼吸散热，忽略
    颤抖产热、出汗及汗液蒸发。
\end{enumerate}

\section{符号说明}
\begingroup
\normalfont\normalsize
\renewcommand{\arraystretch}{1.18}
\setlength{\tabcolsep}{6pt}
\begin{longtable}{p{3.0cm}<{\centering}p{7.5cm}<{\centering}p{2.4cm}<{\centering}}
    \caption{主要符号说明}
    \label{tab:q1-symbols}\\
    \toprule[1.5pt]
    符号 & 含义 & 单位\\
    \midrule[1pt]
    \endfirsthead
    \multicolumn{3}{c}{表~\thetable\quad 主要符号说明（续）}\\
    \toprule[1.5pt]
    符号 & 含义 & 单位\\
    \midrule[1pt]
    \endhead
    \midrule[1pt]
    \multicolumn{3}{r}{续下页}\\
    \endfoot
    \bottomrule[1.5pt]
    \endlastfoot
    $T_c,T_s$ & 人体核心温度、平均皮肤温度 & $^{\circ}\mathrm C$\\
    $T_1,T_2,T_3$ & 防护服内层、PCM 层和外层节点温度 & $^{\circ}\mathrm C$\\
    $T_{3,j}$ & 第 $j$ 个外层涂层节点温度 & $^{\circ}\mathrm C$\\
    $T_\infty$ & 环境温度 & $^{\circ}\mathrm C$\\
    $\alpha_s$ & 皮肤节点占人体总质量的等效比例 & --\\
    $\dot V_{bl},\dot V_{bl,\mathrm{eq}}$ & 实际皮肤血流量、目标皮肤血流量
    & $\mathrm{L/(m^2h)}$\\
    $M_0,M$ & 静止工况、当前活动工况的单位面积代谢产热率 & $\mathrm{W/m^2}$\\
    $h_{\mathrm{in}},h_{\mathrm{out}}$ & 服装内侧、外侧的对流换热系数
    & $\mathrm{W/(m^2K)}$\\
    $q_{ij}$ & 节点 $i$ 到节点 $j$ 的热流密度 & $\mathrm{W/m^2}$\\
    $c_{\mathrm{app}}(T)$ & PCM 温度相关表观比热 & $\mathrm{J/(kg\,K)}$\\
    $v$ & 外界风速 & $\mathrm{m/s}$\\
    $t_{15},t_{10}$ & 两个皮肤温度阈值的首次到达时刻 & $\mathrm s$\\
    $N$ & 外层涂层总层数 & --\\
    $\delta_{\mathrm{out}}(N)$ & 层数为 $N$ 时的外层总厚度 & $\mathrm m$\\
    $J(N)$ & 综合站立时间 & $\mathrm s$\\
\end{longtable}
\endgroup


\section{问题一模型的建立与求解}

\subsection{问题一分析}

问题一旨在预测实验者在 $-40\,^{\circ}\mathrm C$ 静止无风环境中的平均皮肤温度演化，
并确定其首次降至 $15\,^{\circ}\mathrm C$ 和 $10\,^{\circ}\mathrm C$ 的时刻。首先，我们将
人体核心、皮肤与三层防护服抽象为集中参数节点，沿核心产热、血流输热、服装导热至
环境散热的路径建立能量平衡；其中，引入动态皮肤血流描述人体低温下的热调节过程，
并采用 PCM 表观比热分析相变过程中的潜热释放，之后，将核心温度、皮肤温度、三层服装温度和实际皮肤血流
组成六状态耦合方程，结合初始温度与自然对流边界进行数值积分。最后，输出平均皮肤温度
首次下降 $15\,^{\circ}\mathrm C$ 和 $10\,^{\circ}\mathrm C$ 的事件时刻，为有效工作时间和危险阈值时间。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/问题一流程图.pdf}
    \caption{问题一建模与求解流程图}
    \label{fig:question1-flowchart}
\end{figure}


\subsection{总体传热结构}

人体核心产生的热量首先传递至皮肤，随后由皮肤进入防护服，并依次经过内层织物、
PCM 功能层和外层隔热层，最终散失到低温环境中。因此，主要传热路径可以表示为
\[
T_c\longrightarrow T_s\longrightarrow T_1\longrightarrow T_2
\longrightarrow T_3\longrightarrow T_\infty.
\]
其中，$T_c$ 和 $T_s$ 分别表示人体核心温度和平均皮肤温度，$T_1,T_2,T_3$ 分别表示
内层织物、PCM 功能层和外层隔热层温度，$T_\infty$ 为环境温度。

\subsection{基础参数}

\subsubsection{人体面积与服装材料参数}

采用 Stevenson 公式计算人体有效换热面积。对身高 $h=170\,\mathrm{cm}$、
体重为 $m_b=60\,\mathrm{kg}$
的实验者，有
\[
A_h=0.0061h+0.0128w-0.1529=1.6521\,\mathrm{m^2}.
\]
衣料面积一般不超过人体表面积的 $1.25$ 倍，故取
\[
A_m=1.25A_h=2.0651\,\mathrm{m^2}
\]
其中，$A_h$ 用于各界面热流计算，$A_m$ 用于服装质量、热容量、潜热及
成本计算。由
\[
m_i=\rho_iA_m\delta_i,\qquad i=1,2,3.
\]
得到三层材料质量分别为
\[
m_1=0.3007\,\mathrm{kg},\qquad
m_2=0.4562\,\mathrm{kg},\qquad
m_3=0.1859\,\mathrm{kg}.
\]

\subsubsection{代谢及呼吸参数}

人体核心是系统内部的主要热源。实验者静止站立时，核心通过代谢产生热量，同时经呼吸
排出显热和潜热，扣除呼吸散热后的剩余热量再向皮肤传递。

将人体单位有效体表面积的代谢产热率记为 $M_0$。ASHRAE 热舒适标准将静止站立工况
对应的代谢率列为
$1.2\,\mathrm{met}$，即 $70\,\mathrm{W/m^2}$，因此
$M_0=70\,\mathrm{W/m^2}$~\cite{ashrae55addendumg}。

呼吸散热由潜热损失与显热损失组成。设 $p_a$ 为环境水汽分压，呼吸散热的经验
关系为~\cite{doherty1988}
\[
\begin{aligned}
q_{\mathrm{res,lat}}&=0.0023M_0(44-p_a),\\
q_{\mathrm{res,sens}}&=0.0014M_0(34-T_\infty),\\
q_{\mathrm{res}}&=q_{\mathrm{res,lat}}+q_{\mathrm{res,sens}}.
\end{aligned}
\]
第一项表示呼吸潜热损失，第二项表示呼吸显热损失。代入
$p_a=0\,\mathrm{Torr}$， $M_0=70\,\mathrm{W/m^2}$ 和
$T_\infty=-40\,^{\circ}\mathrm C$，分别得到
\[
q_{\mathrm{res,lat}}=7.084\,\mathrm{W/m^2},\qquad
q_{\mathrm{res,sens}}=7.252\,\mathrm{W/m^2},\qquad
q_{\mathrm{res}}=14.336\,\mathrm{W/m^2}.
\]

核心剩余热量通过核心--皮肤热流 $q_{cs}$ 输送至皮肤。

\subsection{人体动态热调节模型}

\subsubsection{皮肤血流动态调节与核心--皮肤传热模型}

Pierce 双节点模型是在 Gagge 人体核心--皮肤双节点热调节理论基础上发展形成的，能够
同时描述核心--皮肤传热及血流调节，因此本文采用其作为人体热调节基础模型
~\cite{gagge1986}。

皮肤温度下降会引起外周血管收缩，实际皮肤血流随之减小，并同时改变核心--皮肤
传热能力和人体热学分区。该调节过程可概括为
\[
T_s\longrightarrow \dot V_{bl,\mathrm{eq}}
\longrightarrow \dot V_{bl}
\longrightarrow G_{cs},\alpha_s
\longrightarrow q_{cs},C_c,C_s.
\]

\textbf{（1）目标皮肤血流。}
Pierce 双节点模型同时考虑核心暖信号和皮肤冷信号对血流的调节，其中皮肤冷信号为
$\max(0,33.7-T_s)$，$33.7\,^{\circ}\mathrm C$ 为中性皮肤温度。针对持续低温暴露，
本文仅保留皮肤冷信号对应的血管收缩项，并取血管收缩系数
$0.5$~\cite{doherty1988,energyplus2022}。将当前皮肤温度对应的目标皮肤血流记为
$\dot V_{bl,\mathrm{eq}}$，同时保留 SET 算法中的生理下限
$0.5\,\mathrm{L/(m^2h)}$~\cite{ashrae55errata2017}，得到
\begin{equation}
\dot V_{bl,\mathrm{eq}}(T_s)
=\max\!\left\{
0.5,\,
\frac{6.3}{1+0.5\max(0,33.7-T_s)}
\right\}.
\label{eq:q1-blood-flow-equilibrium}
\end{equation}

\textbf{（2）实际血流动态。}
目标血流表示人体趋向达到的调节状态，实际血管收缩存在有限响应时间，因此采用一阶动态过程描述这一响应，
响应时间为$\tau_{bl}=173\,\mathrm s$ ~\cite{wu2017}。据此将实际皮肤血流
$\dot V_{bl}$ 作为第六个动态状态，建立
\begin{equation}
\frac{\mathrm d\dot V_{bl}}{\mathrm dt}
=\frac{\dot V_{bl,\mathrm{eq}}(T_s)-\dot V_{bl}}{\tau_{bl}}.
\label{eq:q1-blood-flow-dynamics}
\end{equation}


\textbf{（3）核心--皮肤传热。}
核心向皮肤传热包括组织导热和血液循环输热，其等效传热系数与相应热流密度为
\begin{equation}
G_{cs}=5.28+1.163\dot V_{bl},\qquad
q_{cs}=G_{cs}(T_c-T_s).
\label{eq:q1-core-skin-heat-transfer}
\end{equation}
其中，$5.28$ 表示组织基础导热，$1.163\dot V_{bl}$ 描述血液循环输热
~\cite{doherty1988,energyplus2022}。

\textbf{（4）核心--皮肤热容量分区。}
实际皮肤血流不仅影响核心向皮肤的传热，还会改变核心与皮肤所承担的
等效热容量。人体组织热容 $0.97\,\mathrm{Wh/(kg\,K)}$ 换算为等效比热
$c_b=3490\,\mathrm{J/(kg\,K)}$~\cite{doherty1988}。记皮肤节点所占的等效热学质量
比例为 $\alpha_s$，则有~\cite{doherty1988,energyplus2022}
\begin{equation}
\alpha_s=0.0417737+
\frac{0.7451832}{\dot V_{bl}+0.585417}.
\label{eq:q1-thermal-partition}
\end{equation}
相应地，核心与皮肤节点的等效热容量分别为
\[
C_c=(1-\alpha_s)m_bc_b,\qquad
C_s=\alpha_sm_bc_b.
\]
由于 $\alpha_s$ 随 $\dot V_{bl}$ 减小而增大，寒冷环境下皮肤血流降低时
皮肤及外周组织节点的等效热容量增大，核心节点的等效热容量相应减小。



\subsubsection{动态热学分区的能量守恒修正}
由于实际皮肤血流 $\dot V_{bl}$ 是动态状态，$\alpha_s$ 及 $C_c,C_s$ 也随时间变化，
进一步考虑热学分区变化对能量守恒的影响
，将热学分区变化等效为核心与皮肤节点之间的质量重新分配。

由 $\alpha_s(\dot V_{bl})$ 及血流动态方程，根据链式法则得到
\[
\dot\alpha_s
=-\frac{0.7451832}{(\dot V_{bl}+0.585417)^2}
\frac{\dot V_{bl,\mathrm{eq}}(T_s)-\dot V_{bl}}{\tau_{bl}},
\]
并定义由核心指向皮肤的等效质量变化率为
\[
\dot m_{cs}=m_b\dot\alpha_s.
\]
其中，$\dot m_{cs}>0$ 表示等效热学质量由核心节点重新划入皮肤节点，
$\dot m_{cs}<0$ 表示反向重新分配。

重新分配的等效质量同时携带显热。取统一参考温度
$T_{\mathrm{ref}}=0\,^{\circ}\mathrm C$，并采用上游节点温度计算其焓流，可得
\[
\dot H_m=
\begin{cases}
c_b(T_c-T_{\mathrm{ref}})\dot m_{cs},&\dot m_{cs}\geq0,\\[4pt]
c_b(T_s-T_{\mathrm{ref}})\dot m_{cs},&\dot m_{cs}<0.
\end{cases}
\]
该焓流将在核心和皮肤两个节点的能量方程中以大小相等、方向相反的形式计入，从而补偿
动态热学分区引起的能量转移，保证人体内部总能量守恒。

\subsubsection{核心--皮肤能量平衡模型}

将皮肤向服装内层输出的热流密度记为 $q_{s1}$，故核心和皮肤节点
的净输入功率为
\[
F_c=A_h(M_0-q_{\mathrm{res}}-q_{cs}),\qquad
F_s=A_h(q_{cs}-q_{s1}).
\]
核心与皮肤的显热储能分别为
\[
U_c=C_c(T_c-T_{\mathrm{ref}}),\qquad
U_s=C_s(T_s-T_{\mathrm{ref}}).
\]
将质量交换焓流按相反方向计入两个控制体，能量守恒写为
\[
\frac{\mathrm dU_c}{\mathrm dt}=F_c-\dot H_m,\qquad
\frac{\mathrm dU_s}{\mathrm dt}=F_s+\dot H_m.
\]
对上述变热容量储能求导，可得温度方程
\begin{equation}
(\dot T_c,\dot T_s)=
\begin{cases}
\left(
\dfrac{F_c}{C_c},
\dfrac{F_s+m_bc_b(T_c-T_s)\dot\alpha_s}{C_s}
\right),&\dot\alpha_s\geq0,\\[10pt]
\left(
\dfrac{F_c+m_bc_b(T_c-T_s)\dot\alpha_s}{C_c},
\dfrac{F_s}{C_s}
\right),&\dot\alpha_s<0.
\end{cases}
\label{eq:q1-human-temperature-balance}
\end{equation}


\subsection{三层防护服传热模型}

\subsubsection{层间热阻网络}

皮肤传出的热量依次经过内层织物、PCM 功能层和外层隔热层，最终由服装外表面散向
环境。静止站立时的内侧换热系数为
$h_{\mathrm{in}}=3.0\,\mathrm{W/(m^2K)}$；无风条件下，外侧自然对流系数为
\[
h_{\mathrm{out}}=2.38|T_3-T_\infty|^{0.25}.
\]

将各层材料的平均温度取在层中心。根据一维导热热阻 $r=\delta/k$ 与对流热阻
$r=1/h$，相邻温度节点之间仅经过各材料的半层厚度，且各段热阻串联，因此有
\begin{equation}
\begin{aligned}
r_{s1}&=\frac{1}{h_{\mathrm{in}}}+\frac{\delta_1}{2k_1},&
r_{12}&=\frac{\delta_1}{2k_1}+\frac{\delta_2}{2k_2},\\
r_{23}&=\frac{\delta_2}{2k_2}+\frac{\delta_3}{2k_3},&
r_{3\infty}&=\frac{\delta_3}{2k_3}+\frac{1}{h_{\mathrm{out}}}.
\end{aligned}
\label{eq:q1-thermal-resistances}
\end{equation}
相应热流密度依次为
\begin{equation}
\begin{aligned}
q_{s1}&=\frac{T_s-T_1}{r_{s1}},&
q_{12}&=\frac{T_1-T_2}{r_{12}},\\
q_{23}&=\frac{T_2-T_3}{r_{23}},&
q_{3\infty}&=\frac{T_3-T_\infty}{r_{3\infty}}.
\end{aligned}
\label{eq:q1-heat-fluxes}
\end{equation}


\subsubsection{PCM 表观热容模型}

PCM 在相变温区内凝固放热，需要将潜热并入温度相关的表观比热。处理过程概括为
\[
\text{原始 DSC}\longrightarrow\text{扣除显热基线}
\longrightarrow q_L(T)\longrightarrow \phi(T),\xi(T)
\longrightarrow c_{\mathrm{app}}(T).
\]
将原始质量归一化热流记为 $q(T)$，以数据温区首尾点连线作为显热基线 $b(T)$，则
纯相变峰为
\[
q_L(T)=\max\{|q(T)|-b(T),0\}.
\]
离散的 $q_L(T)$ 采用 PCHIP 保形插值。由无相变区
$|q_{\mathrm{base}}|=c_2\beta/60000$ 反演扫描速率，得到
\[
\beta=\frac{60000|q_{\mathrm{base}}|}{c_2}\simeq10\,\mathrm{K/min}.
\]
设 DSC 数据温区为 $[T_a,T_b]$。为描述潜热在温度区间内的释放位置，将纯相变峰
归一化为潜热分布函数
\[
\phi(T)=
\frac{q_L(T)}
{\displaystyle\int_{T_a}^{T_b}q_L(\theta)\,\mathrm d\theta},
\qquad
\int_{T_a}^{T_b}\phi(T)\,\mathrm dT=1.
\]
降温凝固时，将温度由 $T_b$ 降至当前温度 $T$ 所完成的相变比例记为
\[
\xi(T)=\int_T^{T_b}\phi(\theta)\,\mathrm d\theta,\qquad
\frac{\mathrm d\xi}{\mathrm dt}
=-\phi(T)\frac{\mathrm dT}{\mathrm dt}.
\]
其中，$T\geq T_b$ 时取 $\xi=0$，$T\leq T_a$ 时取 $\xi=1$。对相变峰积分得到单位
质量潜热
\[
L=\frac{60}{\beta}\int_{T_a}^{T_b}q_L(T)\,\mathrm dT
\approx122.3\,\mathrm{kJ/kg}.
\]

将 PCM 层累计释放的潜热记为
$Q_{\mathrm{lat}}=m_2L\xi(T_2)$。在相变温区内，其释放功率为
\[
\dot Q_{\mathrm{lat}}
=m_2L\frac{\mathrm d\xi}{\mathrm dt}
=-m_2L\phi(T_2)\dot T_2.
\]
PCM 节点的显热平衡为
\[
m_2c_2\dot T_2
=A_h(q_{12}-q_{23})+\dot Q_{\mathrm{lat}}.
\]
将潜热释放功率移至储能项，得到
\[
m_2\bigl[c_2+L\phi(T_2)\bigr]\dot T_2
=A_h(q_{12}-q_{23}).
\]
因此定义表观比热；将 $L$ 换算为 $\mathrm{J/kg}$ 并代入其 DSC 积分式，可写成
\begin{equation}
c_{\mathrm{app}}(T)
=c_2+L\phi(T)
=c_2+\frac{60000}{\beta}q_L(T).
\label{eq:q1-apparent-heat-capacity}
\end{equation}
数据温区外 $\phi(T)=0$，故取
$c_{\mathrm{app}}(T)=c_2=2400\,\mathrm{J/(kg\,K)}$。

图中左图展示了原始 DSC 信号经显热基线分离后得到的潜热峰，右图展示了该潜热峰
换算得到的温度相关表观比热，可以看出PCM 的潜热主要集中在相变温区内释放。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/problem1_pcm_characteristics.pdf}
    \caption{DSC 基线分离与 PCM 表观比热曲线}
    \label{fig:q1-pcm-characteristics}
\end{figure}

\subsubsection{三层材料能量平衡}

各服装节点满足流入热量减去流出热量等于储能变化，因此三层材料的能量方程为
\begin{equation}
\begin{aligned}
m_1c_1\dot T_1&=A_h(q_{s1}-q_{12}),\\
m_2c_{\mathrm{app}}(T_2)\dot T_2&=A_h(q_{12}-q_{23}),\\
m_3c_3\dot T_3&=A_h(q_{23}-q_{3\infty}).
\end{aligned}
\label{eq:q1-clothing-energy-balance}
\end{equation}

\subsection{六状态耦合模型}

取状态向量
\[
\bm x(t)=[T_c,T_s,T_1,T_2,T_3,\dot V_{bl}]^{\mathsf T},
\]
联立式\eqref{eq:q1-blood-flow-equilibrium}--\eqref{eq:q1-blood-flow-dynamics}、
式\eqref{eq:q1-human-temperature-balance}以及式\eqref{eq:q1-thermal-resistances}--
\eqref{eq:q1-clothing-energy-balance}，得到六状态耦合模型
\begin{equation}
\left\{
\begin{aligned}
(\dot T_c,\dot T_s)
&=
\begin{cases}
\left(
\dfrac{F_c}{C_c},
\dfrac{F_s+m_bc_b(T_c-T_s)\dot\alpha_s}{C_s}
\right),&\dot\alpha_s\geq0,\\[10pt]
\left(
\dfrac{F_c+m_bc_b(T_c-T_s)\dot\alpha_s}{C_c},
\dfrac{F_s}{C_s}
\right),&\dot\alpha_s<0,
\end{cases}\\
\dot T_1&=\dfrac{A_h(q_{s1}-q_{12})}{m_1c_1},\\
\dot T_2&=\dfrac{A_h(q_{12}-q_{23})}
{m_2c_{\mathrm{app}}(T_2)},\\
\dot T_3&=\dfrac{A_h(q_{23}-q_{3\infty})}{m_3c_3},\\
\frac{\mathrm d\dot V_{bl}}{\mathrm dt}
&=\dfrac{\dot V_{bl,\mathrm{eq}}(T_s)-\dot V_{bl}}{\tau_{bl}}.
\end{aligned}
\right.
\label{eq:q1-six-state-system}
\end{equation}

\subsection{初始与边界条件}

实验者在 \(t=0\) 时进入低温环境，外界温度变为
 $T_\infty=-40\,^{\circ}\mathrm C$。测试初始人体温度为
$37\,^{\circ}\mathrm C$，故取
\[
T_c(0)=T_s(0)=37\,^{\circ}\mathrm C.
\]
服装三层节点初温与人体相同，取
\[
T_1(0)=T_2(0)=T_3(0)=37\,^{\circ}\mathrm C.
\]
此时皮肤冷信号为零，目标皮肤血流为中性值
$6.3\,\mathrm{L/(m^2h)}$~\cite{doherty1988,energyplus2022}。
因此初始状态向量为
\[
\bm x(0)=[37,37,37,37,37,6.3]^{\mathsf T}.
\]

\subsection{模型求解}

\textbf{Step 1：输入模型参数与初始状态。}
将人体和服装参数、四段热阻关系、PCM 表观比热函数以及初始状态
$\bm x(0)$ 传入六状态模型，设置环境温度在 $t=0$ 时降至
$-40\,^{\circ}\mathrm C$。

\textbf{Step 2：计算六状态方程右端。}
每次计算先由当前 $T_s$ 得到目标血流和实际血流变化率，再由实际血流更新
$G_{cs}$、$\alpha_s$ 与 $\dot\alpha_s$。随后依次计算核心--皮肤热流、四段服装热流及
当前 $T_2$ 对应的 $c_{\mathrm{app}}(T_2)$，最终得到人体两节点、服装三节点和实际血流
共六个状态的导数。基准计算采用自适应 RK45 方法，设置
$\mathrm{rtol}=10^{-7}$、$\mathrm{atol}=10^{-9}$、
最大步长 $5\,\mathrm s$，计算上限为 $24\,\mathrm h$。

\textbf{Step 3：定位阈值事件并复核精度。}
定义下降方向事件
\begin{equation}
g_{15}(t,\bm x)=T_s-15,\qquad
g_{10}(t,\bm x)=T_s-10.
\label{eq:q1-threshold-events}
\end{equation}
由求解器连续延拓得到两个事件的首次过零时刻；检测到 $10\,^{\circ}\mathrm C$
事件后终止积分。加严计算仍采用 RK45 方法，
$\mathrm{rtol}=10^{-9}$、$\mathrm{atol}=10^{-11}$，
最大步长减小为 $1\,\mathrm s$，用于复核事件定位精度。

\subsection{结果与分析}

\subsubsection{温度响应与阈值时间}


\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/problem1_temperature_response.pdf}
    \caption{极寒环境下人体与防护服各节点温度响应}
    \label{fig:q1-temperature-response}
\end{figure}


\begin{table}[H]
    \centering
    \small
    \caption{人体低温阈值}
    \label{tab:q1-six-state-results}
    \begin{tabular}{cccccc}
        \toprule[1.5pt]
        皮肤阈值/$^{\circ}\mathrm C$ & 时刻/s & 时刻/min
        & 核心温度/$^{\circ}\mathrm C$ & \makecell{实际血流\\$\mathrm{L/(m^2h)}$}
        & $\alpha_s$\\
        \midrule[1pt]
        15 & 40717.233 & 678.621 & 26.511 & 0.6101 & 0.6651\\
        10 & 60010.271 & 1000.171 & 21.036 & 0.5000 & 0.7283\\
        \bottomrule[1.5pt]
    \end{tabular}
\end{table}

平均皮肤温度约在 $678.62\,\mathrm{min}$，即 $11.31\,\mathrm h$ 后降至
$15\,^{\circ}\mathrm C$，此后正常工作开始受到影响；约在
$1000.17\,\mathrm{min}$，即 $16.67\,\mathrm h$ 后降至
$10\,^{\circ}\mathrm C$，达到危险阈值。

皮肤温度在进入低温环境后立即下降，
核心温度则先在约 $48.3\,\mathrm{min}$ 时小幅升至 $37.370\,^{\circ}\mathrm C$，随后缓慢
降低。外界冷负荷首先作用于皮肤，人体核心依靠自身热容量
和代谢产热延缓降温，因此皮肤温度与核心温度存在时间尺度的差异。冷扰动由外层向内层传播，因此
三层服装节点在暴露初期迅速降温，并在全时程保持
$T_1>T_2>T_3$，对应。

\subsubsection{血流收缩与热容量重分配}

随着皮肤降温，目标皮肤血流迅速下降，而实际血流受
$\tau_{bl}=173\,\mathrm s$ 的动态响应限制，在暴露初期表现出明显滞后，随后逐渐趋近
目标值。当皮肤温度降至
$15\,^{\circ}\mathrm C$ 和 $10\,^{\circ}\mathrm C$ 时，实际血流分别降至
$0.6101\,\mathrm{L/(m^2h)}$ 和 $0.5000\,\mathrm{L/(m^2h)}$。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{figures/problem1_blood_flow_regulation.pdf}
    \caption{皮肤血流动态调节与等效质量比例演化}
    \label{fig:q1-blood-flow-regulation}
\end{figure}

与此同时，皮肤等效质量比例 $\alpha_s$ 随血流收缩由初始 $0.15$ 增至 $0.7283$。
因此，血流收缩一方面降低核心--皮肤传热能力，减少核心向外周的热量输送；
另一方面改变核心与皮肤的等效热容量分配，从而形成对低温暴露的动态调节。

\subsubsection{PCM 相变热容与潜热释放过程}



\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.82\textwidth]{figures/problem1_pcm_time_process.pdf}
    \caption{PCM 表观比热与累计潜热释放的时间过程}
    \label{fig:q1-pcm-time-process}
\end{figure}

PCM 在低温暴露初期存在潜热释放过程，其表观比热与累计潜热随时间发生变化。
PCM 约在 $60\,\mathrm s$ 时进入主要相变区间，并于约 $225\,\mathrm s$ 时基本完成。
相变过程中，表观比热先升高后降低，在约 $130\,\mathrm s$ 时达到峰值
$22.680\,\mathrm{kJ/(kg\cdot K)}$；与此同时，累计潜热持续增加，最终稳定在约
$55.8\,\mathrm{kJ}$。由此可见，PCM 的主要作用集中在进入极寒环境后的最初数分钟，
通过释放有限潜热缓冲初始冷冲击；相变结束后，长期耐寒过程主要由人体产热及防护服
隔热性能共同决定。

\Needspace{8\baselineskip}
\subsubsection{逐级传热路径的时间演化}


\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.98\textwidth]{figures/problem1_heat_flux_pathways.pdf}
    \caption{人体--防护服逐级传热路径的热流密度时间演化}
    \label{fig:q1-heat-flux-pathways}
\end{figure}

热量由人体经皮肤及三层服装向环境传递，
在 $t=0$ 时，人体与服装各节点均为 $37\,^{\circ}\mathrm C$，内部温差为零，
而外层与环境存在 $77\,\mathrm K$ 温差，使 $q_{3\infty}$ 瞬时升至
$532.184\,\mathrm{W/m^2}$。随后冷扰动由外向内传播，各段热流依次建立。除初始时刻外，
前 $10\,\mathrm{min}$ 内总体满足
$q_{3\infty}>q_{23}>q_{12}>q_{s1}>q_{cs}$，其中 $q_{12}$ 的阶段性变化反映了 PCM
相变潜热对热流传递的缓冲作用。

约 $30\,\mathrm{min}$ 后，$q_{s1}$、$q_{12}$、$q_{23}$ 和 $q_{3\infty}$ 均接近
$133\,\mathrm{W/m^2}$，表明服装传热链逐渐进入准稳态；此时 $q_{cs}$ 约为
$51.1\,\mathrm{W/m^2}$，仍受核心--皮肤温差及动态血流调节影响。总体来看，外界冷冲击
首先消耗服装与 PCM 储热；服装侧进入准稳态后，环境散热逐渐转由人体显热储备和
代谢产热经皮肤与服装持续补给。

\subsection{模型检验与敏感性分析}


\subsubsection{血流迟滞的结构检验}

改变 $\tau_{bl}$ 所得阈值时间见表~\ref{tab:q1-tau-check}。当
$\tau_{bl}=1\,\mathrm s$ 时，六状态结果已接近五状态模型，验证了
$\tau_{bl}\to0$ 时恢复准稳态血流的退化关系。

\begin{table}[H]
    \centering
    \caption{皮肤血流响应时间常数的结构敏感性}
    \label{tab:q1-tau-check}
    \begin{tabular}{ccc}
        \toprule[1.5pt]
        $\tau_{bl}$/s & $t_{15}$/s & $t_{10}$/s\\
        \midrule[1pt]
        1   & 40725.231 & 59983.661\\
        60  & 40722.456 & 59992.676\\
        173 & 40717.233 & 60010.271\\
        200 & 40716.006 & 60014.542\\
        \bottomrule[1.5pt]
    \end{tabular}
\end{table}

与五状态准稳态模型相比，六状态主模型的 $t_{15}$ 提前 $7.91\,\mathrm s$，
$t_{10}$ 延后 $26.99\,\mathrm s$，相对变化分别为 $-0.019\%$ 和 $0.045\%$。
动态血流使早期实际血流高于目标值，同时减小皮肤等效热容量的增长速度；
两种作用在整个暴露过程中部分抵消，因此加入血流迟滞改善了生理过程描述，
但没有改变本问的小时量级结论。

在 $60\sim200\,\mathrm s$ 范围内，$t_{15}$ 和 $t_{10}$ 相对主模型的变化均不超过
$0.03\%$。响应变慢时，实际血流在降温初期维持较高水平，核心向皮肤输热增强；
与此同时 $\alpha_s$ 增长较慢，皮肤等效热容量较小。两条路径方向相反，使最终阈值
对 $\tau_{bl}$ 的变化不敏感。

\subsubsection{换热与产热参数敏感性}

对六状态主模型的基础代谢率、内侧换热系数和外侧自然对流倍率分别作
$\pm10\%$ 扰动，得到表~\ref{tab:q1-six-sensitivity}。

\begin{table}[H]
    \centering
    \small
    \caption{六状态主模型的单因素敏感性}
    \label{tab:q1-six-sensitivity}
    \begin{tabular}{ccccc}
        \toprule[1.5pt]
        参数 & $-10\%$ 时 $\Delta t_{15}$ & $+10\%$ 时 $\Delta t_{15}$
        & $-10\%$ 时 $\Delta t_{10}$ & $+10\%$ 时 $\Delta t_{10}$\\
        \midrule[1pt]
        $h_{\mathrm{in}}$ & $+16.06\%$ & $-11.57\%$
        & $+16.90\%$ & $-11.93\%$\\
        $M_0$ & $-8.28\%$ & $+10.21\%$
        & $-9.76\%$ & $+12.49\%$\\
        $\lambda_{\mathrm{out}}$ & $+7.31\%$ & $-5.68\%$
        & $+7.70\%$ & $-5.92\%$\\
        \bottomrule[1.5pt]
    \end{tabular}
\end{table}

内侧换热系数的影响最大：$h_{\mathrm{in}}$ 增大使皮肤至内层的热阻减小，两个阈值
均明显提前。代谢率通过改变净产热直接控制耐寒时间，外侧自然对流倍率则通过改变
服装向环境的散热影响阈值。服装内、外侧换热条件的不确定性对结论的影响明显大于
血流响应时间常数，应在实际应用中优先标定。

\subsubsection{集中参数近似检验}

初始工况下内层和外层 Biot 数分别约为 0.031 和 0.040，均小于集中参数法常用判据
$Bi<0.1$；三层材料的非相变热扩散特征时间约为
$7.2\,\mathrm s$、$3.5\,\mathrm s$ 和 $2.8\,\mathrm s$。服装层厚度处于亚毫米量级，
层内温度调整远快于人体降温过程，因此每层可以采用一个平均温度节点。
\section{问题二模型的建立与求解}

\subsection {问题分析}
问题二要求在问题一模型的基础上，进一步考虑空气流动和人体运动对传热过程的影响。我们需要修正人体表面—内层织物和外层隔热层—外部环境两个空气边界层的对流换热条件，同时，轻微运动还会提高人体代谢产热，并相应增加呼吸显热与潜热损失。最终得到该情况下的室外停留时间。在此基础上重新求解人体—防护服耦合传热过程，得到实验者在该工况下的室外安全停留时间。
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/问题二机制图.pdf}
    \caption{问题二优化机制图}
    \label{q2_flow}
\end{figure}
\subsection {有风轻微运动条件下低温防护服耦合传热模型的建立}

\subsubsection{轻微运动下的内侧对流修正}
轻微运动会使皮肤与内层织物之间的空气层发生扰动。轻微运动工况下，将内侧等效对流换热系数取为

$$
h_{\rm in}=4.0\,\mathrm{W/(m^2K)}.
$$

从皮肤表面到内层织物中心，热量首先经过衣内空气层对流，再经过内层织物的半层导热。由于两段传热过程串联，单位面积总热阻为

\begin{equation}
r_{s1}=\frac{1}{h_{\rm in}}+\frac{\delta_1}{2k_1}.
\end{equation}

根据热流密度等于温差除以热阻，可得皮肤向内层织物的热流密度：

\begin{equation}
q_{s1}=\frac{T_s-T_1}{r_{s1}}.
\end{equation}

\subsubsection{外界风作用下的外侧混合对流}
由于长城站存在$v=3m/s$的外部风速，外层隔热层与环境之间的换热需要使用强制对流经验式进行定量表示。我们使用Fanger 热舒适模型，有：

\begin{equation}
h_{\rm forced}=12.1\sqrt v,
\end{equation}

该关系表征外部气流不断破坏服装表面的热边界层，使边界层厚度减小，从而增强服装表面与环境之间的对流换热。

自然对流和强制对流可能同时存在。按照Fanger/ASHRAE 对流换热模型，当二者同时存在时，取其中较大的一个作为实际对流换热系数 ：
\begin{equation}
h_{\rm out}=\max\left\{h_{\rm nat},h_{\rm forced}\right\},
\end{equation}
意味着实际换热由占主导地位的对流机制控制。

从外层隔热材料中心到环境，传热过程由外层材料半层导热和表面对流串联组成，故单位面积总热阻为

\begin{equation}
r_{3\infty}=\frac{\delta_3}{2k_3}+\frac{1}{h_{\rm out}}.
\end{equation}
最终，外层向环境的热流密度由外层—环境温差除以该总热阻得到：
\begin{equation}
q_{3\infty}=\frac{T_3-T_\infty}{r_{3\infty}}.
\end{equation}

\subsubsection{轻微运动下的代谢与呼吸散热模型}
轻微运动会提高人体单位面积代谢率。ANSI/ASHRAE Standard 55 将“站立、轻度活动”列为约 $1.6\,\mathrm{met}$ 的典型活动水平[3]。按照 $1\,\mathrm{met}=58.2\,\mathrm{W/m^2}$ 的换算关系，该参数可由下式得到：

$$
M_{\rm act}=1.6\times58.2\approx93\,\mathrm{W/m^2}.
$$

因此本文令 $M=M_{\rm act}$。该取值用于将题目中的“轻微运动”转化为可计算的代谢强度，是标准活动表中的典型值，并非实验者的个体实测值。

代谢率升高会增加人体内部产热，同时也会提高呼吸散热。本文沿用 Gagge/Pierce 两节点模型的呼吸热损失经验关系[4]。其中呼吸潜热损失由代谢率和空气水汽分压共同决定：

$$
q_{\rm res,lat}=0.0023M(44-p_a).
$$

呼吸显热损失由代谢率和环境温度共同决定：

$$
q_{\rm res,sens}=0.0014M(34-T_\infty).
$$

上述关系中的经验系数 $0.0023$ 和 $0.0014$ 均来自该两节点人体热平衡模型[4]；使用时 $M$ 以 $\mathrm{W/m^2}$ 计，$p_a$ 以 $\mathrm{Torr}$ 计，$T_\infty$ 以 $^\circ\mathrm C$ 计。由于两项均随 $M$ 变化，修改代谢率后必须同步重新计算呼吸潜热与显热损失。

将两部分相加，得到单位面积呼吸总散热：

$$
q_{\rm res}=q_{\rm res,lat}+q_{\rm res,sens}.
$$

因此，人体核心节点接收的总代谢功率为

$$
\dot Q_{\rm met}=A_hM,
$$

扣除呼吸散热后的净热输入为

$$
\dot Q_{\rm net}=A_h(M-q_{\rm res}).
$$

\subsection {模型求解与结果分析}
\subsubsection{初始条件、数值方法与事件判定}
初始时假设人体与服装已在室内达到相同温度，因此取
\begin{equation}
T_c(0)=T_s(0)=T_1(0)=T_2(0)=T_3(0)=37\,{}^\circ\mathrm C.
\end{equation}
初始实际皮肤血流量取静息基准值
\begin{equation}
\dot V_{bl}(0)=6.3\,\mathrm{L/(m^2h)}.
\end{equation}

本文采用 \texttt{solve\_ivp} 对六状态方程组进行数值积分，基准求解器选择
RK45，相对误差限和绝对误差限分别设为 $10^{-7}$ 和 $10^{-9}$，最大积分步长
取 $5\,\mathrm s$。为识别皮肤温度向下穿越两个阈值的时刻，分别定义事件函数
\begin{equation}
g_{15}(t,\bm{x})=T_s(t)-15,
\end{equation}
\begin{equation}
g_{10}(t,\bm{x})=T_s(t)-10.
\end{equation}
两个事件函数的检测方向均设为 $-1$，只记录降温过程中的穿越。事件时间由求解器
的连续插值确定，而非由每 $5\,\mathrm s$ 输出一次的离散采样点近似读取；若阈值在
$24\,\mathrm h$ 的计算范围内未被触发，则将相应事件记为“未达到”。

\subsubsection{返回时间与阈值状态}
在环境温度为 $-40\,{}^\circ\mathrm C$、风速为 $3\,\mathrm{m/s}$ 且实验者进行轻微运动的条件下，皮肤温度首次下降至 $15\,{}^\circ\mathrm C$ 的时间为

$$
t_{15}=17565.379485\,\mathrm s=292.756325\,\mathrm{min}.
$$
因此，在本文模型与安全阈值定义下，实验者应在室外停留约 $293\,\mathrm{min}$ 时返回室内。

皮肤温度继续下降至 $10\,{}^\circ\mathrm C$ 的时间为

$$
t_{10}=26317.359901\,\mathrm s=438.622665\,\mathrm{min}.
$$

除皮肤温度外，阈值时刻的人体核心、PCM 和皮肤血流状态如表
\ref{tab:q2-threshold-state} 所示。
\begin{table}[H]
    \centering
    \small
    \caption{问题二主工况的阈值时间与关键状态}
    \label{tab:q2-threshold-state}
    \begin{tabular}{cccccc}
        \toprule[1.5pt]
        阈值 & 时刻/s & 时刻/min & $T_c/{}^\circ\mathrm C$
        & $\dot V_{bl}/[\mathrm{L/(m^2h)}]$ & $\xi$\\
        \midrule[1pt]
        $T_s=15\,{}^\circ\mathrm C$ & 17565.379 & 292.756 & 31.732 & 0.612 & 1.000\\
        $T_s=10\,{}^\circ\mathrm C$ & 26317.360 & 438.623 & 26.260 & 0.500 & 1.000\\
        \bottomrule[1.5pt]
    \end{tabular}
\end{table}
由表可见，达到 $15\,{}^\circ\mathrm C$ 阈值时核心温度已降至
$31.732\,{}^\circ\mathrm C$，实际皮肤血流也已接近血管收缩状态下的低值；达到
$10\,{}^\circ\mathrm C$ 时核心温度进一步降至 $26.260\,{}^\circ\mathrm C$。
因此，$t_{10}$ 仅用于刻画更低温度风险边界，不能作为建议继续工作的时间。

\subsubsection{温度演化、外侧对流与 PCM 放热结果}
问题二完整工况下五个温度节点的演化如图~\ref{fig:q2-six-state-temperature} 所示。
\begin{figure}[H]
    \centering
    \includegraphics[width=0.84\textwidth]{../results/figures/fig4_q2_six_state_temperature.pdf}
    \caption{问题二完整工况下人体与服装温度演化}
    \label{fig:q2-six-state-temperature}
\end{figure}

外层隔热层、PCM 层和内层织物进入极寒环境后迅速降温，并在较短时间内接近各自
的准稳态温度；皮肤和核心温度则在人体较大热容量及核心--皮肤换热的共同作用下缓慢下降。

将 $v=3\,\mathrm{m/s}$ 代入强制对流经验式可得
\begin{equation}
h_{\rm forced}=12.1\sqrt{3}=20.957815\,\mathrm{W/(m^2K)}.
\end{equation}
时间序列中自然对流换热系数的最大值为
$7.050171\,\mathrm{W/(m^2K)}$，始终小于强制对流换热系数，因此整个计算时段的
外侧换热均由强制对流控制。与问题一相比，服装外表面的对流热阻显著减小，形成了
问题二降温加快的主要外部原因。

PCM 节点约在 $20\,\mathrm s$ 时进入相变区，并约在 $80\,\mathrm s$ 时完成相变。
由 PCM 质量与单位质量潜热相乘，潜热释放上限为
\begin{equation}
Q_{\rm PCM,max}=m_2L=55802.490579\,\mathrm J.
 \end{equation}
两个阈值时刻的相变进度均为 $\xi=1$，说明 PCM 潜热在暴露早期已经全部释放。

\subsubsection{外界风、衣内换热与代谢的效应拆分}
为识别问题二三项修正的独立作用，设置 Q1、W、I、M 和 Q2 五个工况。其中 Q1
为问题一无风静止基线；W 仅加入外界风；I 仅将内侧对流换热系数提高至
$4.0\,\mathrm{W/(m^2K)}$；M 仅将代谢率提高至 $93\,\mathrm{W/m^2}$；Q2
同时引入全部问题二条件。以 Q1 为基准，第 $j$ 个工况的相对变化率定义为
\begin{equation}
\Delta t_{15}^{(j)}=
\frac{t_{15}^{(j)}-t_{15}^{(\mathrm{Q1})}}{t_{15}^{(\mathrm{Q1})}}\times100\%.
\end{equation}
各工况结果见表~\ref{tab:q2-effect-decomposition} 和图
\ref{fig:q2-effect-decomposition}。

\begin{table}[H]
    \centering
    \small
    \caption{问题二各修正因素对返回时间的效应拆分}
    \label{tab:q2-effect-decomposition}
    \begin{tabular}{clrr}
        \toprule[1.5pt]
        工况 & 条件变化 & $t_{15}$/min & 相对 Q1 变化\\
        \midrule[1pt]
        Q1 & 无风，$h_{\rm in}=3.0$，$M=70$ & 678.621 & $0$\\
        W  & 仅令 $v=3\,\mathrm{m/s}$ & 379.387 & $-44.094\%$\\
        I  & 仅令 $h_{\rm in}=4.0\,\mathrm{W/(m^2K)}$ & 481.040 & $-29.115\%$\\
        M  & 仅令 $M=93\,\mathrm{W/m^2}$ & 991.325 & $+46.079\%$\\
        Q2 & 同时引入全部问题二条件 & 292.756 & $-56.860\%$\\
        \bottomrule[1.5pt]
    \end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth]{../results/figures/fig6_q2_t15_effect_decomposition.pdf}
    \caption{问题二各修正因素的 $t_{15}$ 效应拆分}
    \label{fig:q2-effect-decomposition}
\end{figure}

单独加入外界风使 $t_{15}$ 缩短 $44.094\%$，单独提高内侧换热系数使其缩短
$29.115\%$，而仅提高代谢率可使其延长 $46.079\%$。完整 Q2 工况相较 Q1
缩短 $56.860\%$，表明运动代谢补热不足以抵消外界风致强制对流和衣内换热增强
造成的综合热损失。三种单因素变化率不能直接线性相加，因为热流随节点温差变化，
皮肤血流及人体热容量动态调整，PCM 有效热容也随温度变化，Q2 反映的是这些机制
非线性耦合后的净效应。
\subsubsection{参数敏感性与稳健性分析}
为消除参数量纲差异，分别对风速 $v$、内侧换热系数 $h_{\rm in}$、代谢率 $M$
和 DSC 扫描速率 $\beta$ 作 $\pm10\%$ 单因素扰动，并定义中心无量纲敏感度
\begin{equation}
S_{t_{15}}^{(p)}=
\frac{t_{15}(1.1p)-t_{15}(0.9p)}{0.2\,t_{15}(p)}.
\end{equation}
计算结果见表~\ref{tab:q2-sensitivity}。
\begin{table}[H]
    \centering
    \small
    \caption{问题二返回时间的局部无量纲敏感度}
    \label{tab:q2-sensitivity}
    \begin{tabular}{ccccc}
        \toprule[1.5pt]
        参数 & $S_{t_{15}}$ & $|S_{t_{15}}|$ & $S_{t_{10}}$ & 影响方向\\
        \midrule[1pt]
        $h_{\rm in}$ & $-1.395080$ & $1.395080$ & $-1.408709$ & 增大则阈值提前\\
        $M$           & $ 0.489214$ & $0.489214$ & $ 0.616572$ & 增大则阈值延后\\
        $v$           & $-0.135420$ & $0.135420$ & $-0.135291$ & 增大则阈值提前\\
        $\beta$       & $-0.002216$ & $0.002216$ & $-0.001501$ & 增大则阈值略提前\\
        \bottomrule[1.5pt]
    \end{tabular}
\end{table}

按 $|S_{t_{15}}|$ 排序，局部敏感性依次为内侧换热系数、代谢率、风速和 DSC
扫描速率。效应拆分中风的全局影响大于内侧换热，而局部敏感性中
$h_{\rm in}$ 最高，二者并不矛盾：前者比较从无风到 $3\,\mathrm{m/s}$ 的工况
切换，后者只考察 Q2 基准点附近的 $\pm10\%$ 小扰动。由于基准工况中强制对流
已占主导，风速的小幅改变只通过平方根关系作用于换热系数；相较之下，
$h_{\rm in}$ 直接改变皮肤至内层织物的主要串联热阻，因此局部影响更强。

对血流响应时间常数分别取 $60\,\mathrm s$、$173\,\mathrm s$ 和
$200\,\mathrm s$，所得返回时间依次为
\begin{equation}
t_{15}=17572.533\,\mathrm s,\quad17565.379\,\mathrm s,\quad17563.797\,\mathrm s.
\end{equation}
三者的最大差异不足 $9\,\mathrm s$。
这表明返回时间对血流响应时间尺度的合理变化较为稳健，而内侧换热和代谢率的标定
更值得优先关注。

\subsubsection{能量守恒与求解器收敛验证}
为检验各节点方程的整体一致性，将人体核心、皮肤和三层服装的储能相加，定义系统
总储能
\begin{equation}
U_{\rm tot}=U_c+U_s+U_1+U_2+U_3,
\end{equation}
其中 PCM 储能通过表观热容对应的焓积分计算。从初始时刻到 $t$ 的外部理论净输入
为代谢产热减去呼吸散热和外层向环境的散热，由此定义能量残差
\begin{equation}
R_E(t)=U_{\rm tot}(t)-U_{\rm tot}(0)
-\int_0^t A_h\left(M-q_{\rm res}-q_{3\infty}\right)\,\mathrm d\tau.
\end{equation}
基准计算的最大绝对能量残差为 $1.914783\,\mathrm J$，相对能量误差为
$3.809929\times10^{-7}$。与全过程的能量交换规模相比，该误差可以忽略，说明
节点热流、动态质量重分配和 PCM 焓值在数值实现中保持一致。

进一步采用 DOP853 求解器，并将相对误差限、绝对误差限和最大步长分别加严至
$10^{-9}$、$10^{-11}$ 和 $1\,\mathrm s$。严格解与基准 RK45 的事件时间差为
\begin{equation}
\left|t_{15}^{\rm DOP853}-t_{15}^{\rm RK45}\right|=0.001306\,\mathrm s,
\end{equation}
\begin{equation}
\left|t_{10}^{\rm DOP853}-t_{10}^{\rm RK45}\right|=0.001332\,\mathrm s.
\end{equation}
该差异远小于论文采用的分钟级报告精度。能量守恒检查验证了方程实现的一致性，
双求解器比较验证了事件时间的数值收敛性，两项结果共同支持问题二求解结果的可靠性。

综上，在 $-40\,{}^\circ\mathrm C$、$3\,\mathrm{m/s}$ 风速和轻微运动条件下，
实验者的预测返回时间为约 $293\,\mathrm{min}$，较问题一无风静止工况缩短
$56.86\%$。该结论适用于本文给定参数和集总传热假设，不应外推为不考虑个体差异、
局部风场及其他生理反应时的无条件医学安全时限。

\section{问题三模型的建立与求解}

\subsection{问题三分析}

问题三旨在确定承重与成本约束下外层涂层的最优厚度，需要同时衡量增厚产生的保温
收益与新增质量造成的站立时间损失。首先，沿用问题一静止、无风工况下的人体--防护服
耦合传热模型，将厚度固定为 $0.3\,\mathrm{mm}$ 的外层总层数作为整数决策变量，并按层
设置温度节点，使外层热阻和热容量随层数同步变化。之后计算各方案的总质量与总成本，利用
$100\,\mathrm{kg}$ 承重上限和原支出 $1.5$ 倍的成本上限筛选可行层数。最后求解各可行
方案的传热过程，确定皮肤温度首次降至 $15\,^{\circ}\mathrm C$ 的时刻，
同时将新增质量按题目给出的承重关系折算为等效时间代价，并与传热时长综合评价，以确定最优涂层厚度。

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/问题三流程图.pdf}
    \caption{问题三建模与求解流程图}
    \label{fig:question3-flowchart}
\end{figure}


\subsection{多外层涂层传热模型}

\subsubsection{决策变量与状态向量}

设外层涂层总层数为 $N\in\mathbb Z_+$，原防护服已有一层，则新增层数为
$N-1$，外层总厚度为
\begin{equation}
    \delta_{\mathrm{out}}(N)=N\delta_3=0.3N\ \mathrm{mm}.
    \label{eq:q3-thickness}
\end{equation}

为同时表示外层总热阻和总热容，对每层涂层分别设置一个平均温度节点。
问题三的状态向量扩展为
\begin{equation}
    \bm{x}_N=
    \left(T_c,T_s,T_1,T_2,T_{3,1},\ldots,T_{3,N},\dot V_{bl}\right)^{\mathsf T},
    \label{eq:q3-state}
\end{equation}
其维数为 $N+5$。

\subsubsection{外层节点的能量平衡}

PCM 层中心与第一个外层节点之间的单位面积热阻为
\begin{equation*}
    r_{2,3_1}=\frac{\delta_2}{2k_2}+\frac{\delta_3}{2k_3},
\end{equation*}
相邻两个外层节点中心之间的热阻为
\begin{equation*}
    r_{3_j,3_{j+1}}=\frac{\delta_3}{k_3},
    \qquad j=1,2,\ldots,N-1.
\end{equation*}

因此，相邻外层节点之间的热流密度为
\begin{equation*}
    q_{3_j,3_{j+1}}
    =\frac{T_{3,j}-T_{3,j+1}}{r_{3_j,3_{j+1}}}.
\end{equation*}

设单层涂层质量为 $m_3$，显热热容为 $C_3=m_3c_3$，则第 $j$ 个
外层节点满足
\begin{equation}
    C_3\frac{\mathrm dT_{3,j}}{\mathrm dt}
    =A_h\left(q_{\mathrm{in},j}-q_{\mathrm{out},j}\right).
    \label{eq:q3-outer-balance}
\end{equation}
最外节点与环境之间继续采用问题一的自然对流边界：
\begin{equation*}
    h_{\mathrm{out}}
    =2.38\lambda_{\mathrm{out}}
    \left|T_{3,N}-T_{\infty}\right|^{0.25},
\end{equation*}
\begin{equation*}
    q_{3_N,\infty}
    =\frac{T_{3,N}-T_{\infty}}
    {\delta_3/(2k_3)+1/h_{\mathrm{out}}}.
\end{equation*}
\subsection{质量、成本与承重约束}

服装材料面积为 $A_m=1.25A_h=2.065125\,\mathrm{m^2}$。由外层密度
$\rho_3=300\,\mathrm{kg/m^3}$ 和单层厚度 $\delta_3=0.3\,\mathrm{mm}$，得单层质量
\begin{equation*}
    m_3=\rho_3A_m\delta_3=0.18586125\,\mathrm{kg}.
\end{equation*}

外层材料单价为 $300$ 元/kg，因此每新增一层的直接材料费为
\begin{equation*}
    \Delta C_3=300m_3=55.758375\,\text{元}.
\end{equation*}

原防护服的材料成本为 $C_0=377.091825$ 元，成本上限为
\begin{equation*}
    C_{\max}=1.5C_0=565.6377375\,\text{元}.
\end{equation*}

层数为 $N$ 时，总质量和总成本分别为
\begin{equation}
    m(N)=m_1+m_2+Nm_3,
    \label{eq:q3-mass}
\end{equation}
\begin{equation}
    C(N)=C_0+(N-1)\Delta C_3.
    \label{eq:q3-cost}
\end{equation}

硬约束整套防护服质量，即
\begin{equation*}
    m(N)\leq 100\,\mathrm{kg}.
\end{equation*}

将式\eqref{eq:q3-cost}代入成本约束，可得预算可行的整数集合为
\begin{equation*}
    N\in\{1,2,3,4\}.
\end{equation*}

\subsection{重量惩罚与优化模型}

设问题一原防护服的最长时间为
$t_0=t_{15}(1)$，增厚带来的纯传热时长增益为 $t_{15}(N)-t_0$。
题目给出每多承受 $0.5\,\mathrm{kg}$ 使站立时间减少 $10\,\mathrm{s}$，
因而时间—重量换算系数为
\begin{equation*}
    \lambda_m=\frac{10}{0.5}=20\,\mathrm{s/kg}.
\end{equation*}

时间惩罚以问题一为基准，只计入新增质量
\begin{equation*}
    \Delta m(N)=m(N)-m(1),
\end{equation*}
因此，问题三的综合站立时间为
\begin{equation}
    J(N)=t_0+\left[t_{15}(N)-t_0\right]-\lambda_m\Delta m(N)
    =t_{15}(N)-20\left[m(N)-m(1)\right].
    \label{eq:q3-objective}
\end{equation}
最终离散优化模型为
\begin{equation}
    \max_N J(N),\qquad
    \mathrm{s.t.}\quad
    \left\{
    \begin{aligned}
        &N\in\{1,2,3,4\},\\
        &C(N)\leq1.5C_0,\\
        &m(N)\leq100\,\mathrm{kg}.
    \end{aligned}
    \right.
    \label{eq:q3-optimization}
\end{equation}

\subsection{模型求解}

参照问题一六状态模型的数值求解流程，本文将外层层数枚举嵌入动态传热方程求解，
对每个可行方案分别积分，再比较目标函数值。

\par\noindent\textbf{Step 1：生成可行方案并设置初始状态。}
由式\eqref{eq:q3-thickness}、式\eqref{eq:q3-mass}和式\eqref{eq:q3-cost}
得到候选集合 $N\in\{1,2,3,4\}$，逐一计算总厚度、总质量与总成本，
筛除不满足预算或承重约束的方案。各方案的核心、皮肤和全部服装节点初温均设为
$37\,^{\circ}\mathrm C$，实际皮肤血流初值为
$6.3\,\mathrm{L/(m^2h)}$。

\par\noindent\textbf{Step 2：建立并积分变维状态方程。}
对给定 $N$，按式\eqref{eq:q3-state}构造 $N+5$ 维状态向量，并按
式\eqref{eq:q3-outer-balance}更新外层节点数、层间热阻和总显热热容。每次计算方程
右端时，先由当前皮肤温度更新目标血流和实际
血流变化率，再计算人体两节点、内层、PCM 层及 $N$ 个外层节点的温度导数。
基准计算采用自适应 RK45 方法，设置
$\mathrm{rtol}=10^{-7}$、$\mathrm{atol}=10^{-9}$、最大步长 $5\,\mathrm s$，
计算上限为 $24\,\mathrm h$。

\par\noindent\textbf{Step 3：定位温度阈值事件。}
定义下降方向事件函数
\begin{equation}
    g_{15}(t,\bm x_N)=T_s-15,\qquad
    g_{10}(t,\bm x_N)=T_s-10.
\label{eq:q3-events}
\end{equation}
求解器利用连续延拓定位式\eqref{eq:q3-events}中两个事件的首次过零时刻，得到
$t_{15}$ 和 $t_{10}$，不从离散输出点插值反推。为检验数值精度，另以
$\mathrm{rtol}=10^{-9}$、$\mathrm{atol}=10^{-11}$ 和最大步长 $1\,\mathrm s$
对最优候选方案加严复算。

\par\noindent\textbf{Step 4：计算评分并选择最优方案。}
在式\eqref{eq:q3-optimization}给出的可行域内，由式\eqref{eq:q3-objective}计算
$J(N)$。比较四个候选方案的评分，取
$J(N)$ 最大者作为问题三的最优外层厚度。

\subsection{结果分析}

\subsubsection{候选厚度方案}

四个可行方案的数值结果如表\ref{tab:q3-candidates}所示。
\begin{table}[H]
    \centering
    \small
    \caption{问题三外层厚度候选方案}
    \label{tab:q3-candidates}
    \begin{tabular}{cccccccc}
        \toprule[1.5pt]
        $N$ & 总厚度/mm & 总质量/kg & 新增质量/kg
        & 总成本/元 & $t_{15}$/min & 惩罚/s & $J$/min\\
        \midrule[1pt]
        1 & 0.3 & 0.942771 & 0       & 377.091825 & 678.621 & 0     & 678.621\\
        2 & 0.6 & 1.128632 & 0.185861& 432.850200 & 696.935 & 3.717 & 696.873\\
        3 & 0.9 & 1.314493 & 0.371723& 488.608575 & 715.724 & 7.434 & 715.600\\
        4 & 1.2 & 1.500355 & 0.557584& 544.366950 & 735.001 & 11.152& \textbf{734.815}\\
        \bottomrule[1.5pt]
    \end{tabular}
\end{table}

由表\ref{tab:q3-candidates}可知，外层每增加一层，
$t_{15}$ 增加约 $18$--$19\,\mathrm{min}$，而重量惩罚仅增加 $3.717\,\mathrm{s}$。
保温收益明显大于新增重量代价，因此预算可行域内的最优解为
\begin{equation*}
    N^*=4,\qquad \delta_{\mathrm{out}}^*=1.2\,\mathrm{mm}.
\end{equation*}
即在原防护服外层新增 3 层涂层。该方案的纯传热时间为
$735.001\,\mathrm{min}$，新增重量惩罚为 $11.152\,\mathrm{s}$，所以重量修正后的主指标为
$734.815\,\mathrm{min}$。相对问题一基准值 $678.621\,\mathrm{min}$，净增益为
$56.194\,\mathrm{min}$。

\subsection{模型检验与敏感性分析}

当 $N=1$ 时，问题三变维模型退化为问题一六状态模型，两者所得
$t_{15}=40717.233\,\mathrm s$ 完全一致。对四层最优方案加严积分容差并将最大步长
由 $5\,\mathrm s$ 减小至 $1\,\mathrm s$ 后，$t_{15}$ 仅改变
$5.11\times10^{-4}\,\mathrm s$；基准求解的相对能量守恒误差为
$1.05\times10^{-7}$，说明层数扩展和事件定位具有良好的数值一致性。

对目标函数的重量惩罚系数扩展分析表明，四层与一层方案发生优劣切换的临界系数约为
\begin{equation*}
    \lambda_m^*\approx6067\,\mathrm{s/kg}.
\end{equation*}
只有当系数由题目值 $20\,\mathrm{s/kg}$ 增大至该量级时，四层方案才会失去优势，因此
最优层数对重量惩罚具有很强稳健性。四层方案对未计入的制造附加费用更敏感：
若对新增材料成本收取的附加率超过 $12.72\%$，四层方案将超过预算，此时应选择三层方案。


\section{问题四模型的建立与求解}
\subsection{问题分析}
问题四要求在不追加材料资金的条件下，仅通过提高中间 PCM 的放热能力，使实验者的室外坚持时间不低于问题三最优方案。由于题目规定 PCM 在各温度下的放热能力同比例提高，因此保持原有相变温区和放热曲线形状不变，引入统一放热倍率对 PCM 相变放热能力进行调整，并以问题三的最长坚持时间作为目标，反演满足要求的最小放热倍率。

\subsection{PCM 放热能力倍率反演模型的建立}
\subsubsection{DSC潜热峰缩放}
设 PCM 基线扣除后的原始放热能力为基准，定义无量纲放热倍率 $\lambda_{\rm pcm}$。
将问题一已经建立的核心—皮肤传热、呼吸散热、层间导热、外侧自然对流和皮肤血流动态关系统一写入右端函数，可将问题四的初值问题概括为

\begin{equation}
\dot{\mathbf x}=\mathbf f\!\left(t,\mathbf x;\lambda_{\rm pcm}\right).
\end{equation}

扣除DSC基线后，改进后的潜热峰定义为

\begin{equation}
q_{L,\lambda}(T)=\lambda_{\rm pcm}q_{L,0}(T).
\end{equation}


\subsubsection{单位质量潜热与表观热容}

DSC 曲线给出单位质量样品的热流，扫描速率 $\beta$ 将时间变化转换为温度变化。对改进后的纯相变峰在整个相变温区积分，并完成毫瓦每毫克、分钟和千克之间的单位换算，可得单位质量潜热：

$$
L_{\lambda}(\beta)=\frac{60000}{\beta}\int_{T_{\min}}^{T_{\max}}q_{L,\lambda}(T)\,dT.
$$
DSC 曲线给出单位质量样品的热流，扫描速率 $\beta$ 将时间变化转换为温度变化。对改进后的纯相变峰在整个相变温区积分，并完成毫瓦每毫克、分钟和千克之间的单位换算，可得单位质量潜热：

$$
L_{\lambda}(\beta)=\frac{60000}{\beta}\int_{T_{\min}}^{T_{\max}}q_{L,\lambda}(T)\,dT.
$$

式中，$60000$ 为单位换算系数，$\beta$ 的单位为 $\mathrm{K/min}$。该式把离散 DSC 放热数据转化为可进入材料能量平衡的单位质量潜热。将潜热峰缩放关系代入积分，并利用常数倍率可移出积分号的线性性质，可进一步得到

\begin{equation}
L_{\lambda}(\beta)=\lambda_{\rm pcm}L_0(\beta).
\end{equation}
在单调降温和单次凝固假设下，潜热释放可等效为相变温区内的附加热容量。将基础比热 $c_p$ 与 DSC 潜热贡献相加，得到改进材料的表观比热：

$$
c_{\rm eff,\lambda}(T)=c_p+\frac{60000}{\beta}q_{L,\lambda}(T).
$$

式中，$c_p$ 是不参与倍率缩放的基础显热比热；第二项只在相变温区内非零。该表观比热直接决定 PCM 温度节点在吸收或释放相同净热功率时的降温速率。

把 $q_{L,\lambda}(T)=\lambda_{\rm pcm}q_{L,0}(T)$ 代入上式，可以整理成

\begin{equation}
c_{\rm eff,\lambda}(T)=c_p+\lambda_{\rm pcm}\left[c_{\rm eff,0}(T)-c_p\right].
\end{equation}

\subsubsection{阈值事件与最小倍率反演模型}
题目以人体表面温度低于 $15\,{}^\circ\mathrm C$ 后工作困难作为比较标准，因此对于给定放热倍率，定义平均皮肤温度首次向下达到该阈值的时间为
$$
t_{15}(\lambda_{\rm pcm})=\inf\left\{t\ge0:T_s(t;\lambda_{\rm pcm})\le15\,{}^\circ\mathrm C\right\}.
$$
问题四没有增加服装质量，所以其比较量直接采用 $t_{15}(\lambda_{\rm pcm})$，不再减去重量惩罚。按照“达到问题三目标且放热倍率尽可能小”的要求，问题四的逆设计模型为
$$
\lambda_{\rm pcm}^*=\min_{\lambda_{\rm pcm}\ge1}\left\{\lambda_{\rm pcm}:t_{15}(\lambda_{\rm pcm})\ge J_3^*\right\}.
$$

\subsection{模型求解与结果分析}
在正式反演前，本文先对若干低倍率候选值进行事件求解，以检查放热倍率增加时 $t_{15}$ 的变化方向。相邻粗搜索点的 $t_{15}$ 增量均为正，其中最小增量为 $30.950405\,\mathrm s$。这说明在已检查区间内，提高 PCM 潜热会延后皮肤温度达到 $15\,{}^\circ\mathrm C$ 的时刻，支持继续向更高倍率扩展根区间并采用单调约束下的标量反演。

Brent 反演得到满足问题三目标的最小 PCM 放热倍率为
$$
\lambda_{\rm pcm}^*=28.352528,
$$
这表示改进材料的纯相变放热能力约为原材料的 $28.352528$ 倍。按照提高百分比的定义，所需新增比例为
$$
\eta=(28.352528-1)\times100\%=2735.253\%.
$$

因此，在本文模型及固定其他物性的假设下，中间功能层的放热能力至少需要比原先提高约 $2735.25\%$。

原材料的单位质量潜热为 $122.312883\,\mathrm{kJ/kg}$。由潜热与倍率的线性关系，改进后的单位质量潜热为

$$
L_{\lambda^*}=28.352528\times122.312883=3467.879499\,\mathrm{kJ/kg}.
$$
\subsubsection{反演曲线与最优倍率分析}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/problem4/problem4_lambda_t15_inverse.pdf}
    \caption{PCM 放热倍率与皮肤温度阈值时间的反演关系}
    \label{fig:q4-lambda-t15-inverse}
\end{figure}

由图\ref{fig:q4-lambda-t15-inverse}可见，在已采样范围内，$t_{15}$ 随
$\lambda_{\rm pcm}$ 增大而持续上升，未出现反向变化；目标水平线与响应曲线在
$\lambda_{\rm pcm}^*=28.353$ 处相交，因此该交点给出了满足时间约束的最小可行
倍率。原材料在 $\lambda_{\rm pcm}=1$ 时的 $t_{15}$ 为
$678.621\,\mathrm{min}$，要达到 $734.815\,\mathrm{min}$ 的目标，需要将纯相变
放热能力提高约 $2735.25\%$。这表明在不增加 PCM 质量、厚度且不改变其他物性的
条件下，仅依靠提高潜热来补偿约 $56.19\,\mathrm{min}$ 的坚持时间差，需要很高的

原 PCM 层的总潜热为 $55.802491\,\mathrm{kJ}$。由于 PCM 层质量保持不变，
最优倍率下整层总潜热增至 $1582.141699\,\mathrm{kJ}$。该数值表示 PCM
在一次完整凝固过程中可释放的总能量上限，而不是瞬时放热功率。将最优倍率代回
六状态模型，得到

\begin{equation*}
    t_{15}=734.814967561\,\mathrm{min},\qquad
    t_{10}=1056.377241\,\mathrm{min}.
\end{equation*}

其中 $t_{15}$ 与目标值之差仅为 $7.09\times10^{-7}\,\mathrm s$，说明反演解准确
落在约束边界上。达到 $15\,{}^\circ\mathrm C$ 皮肤阈值时，核心温度为
$26.511699\,{}^\circ\mathrm C$，PCM 节点温度为
$-19.379422\,{}^\circ\mathrm C$，实际皮肤血流量约为
$0.610140\,\mathrm{L/(m^2h)}$，相变进度已经达到 $1$。这说明 PCM 潜热在皮肤达到
阈值前已经全部释放，后续温度响应仍受人体产热、服装热阻与环境散热共同控制。


\subsubsection{参数敏感性与设计稳健性}

为判断材料指标对模型参数的依赖程度，本文对内侧换热系数 $h_{\rm in}$、基础代谢率
$M_0$、外侧自然对流倍率 $\lambda_{\rm out}$ 和 DSC 扫描速率 $\beta_{\rm DSC}$
分别施加上下扰动，并在每个情景下重新反演达到同一目标所需的最小放热倍率。
图\ref{fig:q4-lambda-sensitivity}给出了最优倍率的归一化敏感度；正值表示参数增大
会提高所需倍率，负值表示参数增大可降低所需倍率。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/problem4/problem4_lambda_sensitivity.pdf}
    \caption{PCM 最优放热倍率的归一化敏感度}
    \label{fig:q4-lambda-sensitivity}
\end{figure}

由图\ref{fig:q4-lambda-sensitivity}可见，$h_{\rm in}$ 的敏感度为 $+12.13$，绝对值
最大，说明皮肤向服装的换热增强会显著增加 PCM 需要补偿的热量；$M_0$ 的敏感度为
$-10.44$，代谢产热提高能够减缓人体降温，因而降低所需放热倍率；
$\lambda_{\rm out}$ 的敏感度为 $+8.58$，表明外界散热增强同样会显著抬高材料
性能要求；$\beta_{\rm DSC}$ 的敏感度为 $+1.00$，与 DSC 换算潜热随扫描速率
反比变化、所需倍率近似同比补偿的关系一致。



\section{模型评价}
\subsection {模型优点}
\begin{enumerate}
    \item 人体核心、皮肤与多层服装之间的传热路径由统一能量方程连接，动态皮肤血流
    同时调节核心--皮肤传热系数和等效热容量分区，并通过质量交换焓流补偿分区变化。
    问题一至问题三基准计算的相对能量守恒误差均处于 $10^{-7}$ 量级。
    \item PCM 的显热与潜热由 DSC 数据构造的温度相关表观比热统一描述，服装外侧采用
    温差相关的自然对流或风速相关的强制对流边界，使相变放热、环境散热和人体产热能够
    在同一非线性状态方程中耦合计算。
    \item 三个问题沿用同一人体--服装模型并逐步增加风速、运动和外层厚度变量。
    问题三在 $N=1$ 时与问题一结果完全一致，严格积分与基准积分所得阈值时间仅相差
    $10^{-3}\,\mathrm s$ 以内，说明模型扩展和数值求解具有良好一致性。
\end{enumerate}

\subsection {模型缺点}
\begin{enumerate}
    \item 人体和各层服装均采用温度均匀的集中参数节点，只描述沿服装厚度方向的平均
    传热，未区分头部、躯干和四肢，也未考虑服装褶皱、局部接触热阻及空气层厚度差异，
    因而难以刻画局部低温区域的温度响应。
    \item 人体热调节仅保留基础代谢、呼吸散热和皮肤血流收缩，未计入颤抖产热、个体
    体脂差异及长时间暴露下的代谢调整。这些简化会影响核心与皮肤之间的热量分配及阈值
    时间估计。
    \item 内侧换热系数、基础代谢率和外侧换热强度对阈值时间较敏感，而现有参数主要
    来自经验公式和典型取值；服装初温及 DSC 扫描速率也缺少针对本实验条件的直接测量，
    因此参数不确定性仍会传递至最终结果。
\end{enumerate}

\subsection{模型改进与推广}
\begin{enumerate}
    \item 可将人体划分为躯干与四肢等多个热学分区，并对每层服装增加有限体积节点，
    进一步加入局部接触热阻、含湿和结霜过程，以获得人体不同部位及材料内部的温度分布。
    \item 可开展低温舱实验，连续测量核心温度、皮肤温度、服装各层温度和热流密度，
    据此联合标定 $h_{\mathrm{in}}$、$h_{\mathrm{out}}$、$\tau_{bl}$ 与代谢率，并通过多名
    实验者的数据建立个体参数分布和结果区间。
    \item 现有状态方程和变层数结构可继续用于时变风速、交替活动强度及不同环境温度，
    重新标定边界换热和人体产热参数后，还可将保温时间、服装质量、成本与活动灵活性
    纳入多目标优化，比较不同材料组合和穿着方案。
\end{enumerate}
\newpage
\begin{thebibliography}{99}
    \bibitem{ashrae55addendumg}
    ASHRAE. ANSI/ASHRAE Addendum g to ANSI/ASHRAE Standard 55-2010:
    Thermal Environmental Conditions for Human Occupancy[S]. Atlanta: ASHRAE, 2013.

    \bibitem{gagge1986}
    Gagge A P, Fobelets A P, Berglund L G. A standard predictive index of human
    response to the thermal environment[J]. ASHRAE Transactions, 1986, 92(2B): 709--731.

    \bibitem{doherty1988}
    Doherty T J, Arens E A. Evaluation of the physiological bases of thermal comfort
    models[J]. ASHRAE Transactions, 1988, 94(1): 1371--1385.

    \bibitem{energyplus2022}
    U.S. Department of Energy. EnergyPlus Engineering Reference, Version 22.1.0[R].
    Washington, DC: U.S. Department of Energy, 2022.

    \bibitem{ashrae55errata2017}
    ASHRAE. Errata Sheet for ANSI/ASHRAE Standard 55-2013: Thermal Environmental
    Conditions for Human Occupancy[S]. Atlanta: ASHRAE, 2017.

    \bibitem{wu2017}
    Wu Y, Nieuwenhoff M D, Huygen F J P M, et al. Characterizing human skin blood
    flow regulation in response to different local skin temperature perturbations[J].
    Microvascular Research, 2017, 111: 96--102.

    \bibitem{pandolf1977}
    Pandolf K B, Givoni B, Goldman R F. Predicting energy expenditure with loads while
    standing or walking very slowly[J]. Journal of Applied Physiology, 1977,
    43(4): 577--581.
        
\end{thebibliography}



\newpage
\begin{appendices}


\section{问题一源代码}
\begin{lstlisting}[language=lingo]


\end{lstlisting}


\section{问题二源代码}
\begin{lstlisting}[language=lingo]

  
\end{lstlisting}

\section{问题三源代码}
\begin{lstlisting}[language=lingo]

\end{lstlisting}

\begin{lstlisting}[language=python]



\end{lstlisting}
\end{appendices}
\end{document}
